[build]
variables = { MISE_PYTHON_GITHUB_ATTESTATIONS = "false" }

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py create_admin $ADMIN_EMAIL $ADMIN_PASSWORD --name EducFarm && gunicorn config.wsgi:application"
